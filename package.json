{
  "name": "control-path-website",
  "version": "1.0.0",
  "description": "Control Path website with CSS-only syntax highlighting",
  "scripts": {
    "highlight": "node highlight.js",
    "build:css": "tailwindcss -i ./input.css -o ./tailwind.css --minify",
    "build": "npm run build:css && npm run highlight"
  },
  "devDependencies": {
    "prismjs": "^1.29.0",
    "tailwindcss": "^3.4.19"
  }
}
